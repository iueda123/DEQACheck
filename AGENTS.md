# このプロジェクトの基本ルール
 
----
 
## このプロジェクトの目的
 
README.md を参照のこと
 
----
 
## 開発中のメインアプリケーション
 
 
`./src/main/java/iu/SpringBoot/Vaadin/Application.java`
 
 
----
 
## ファイル、フォルダの運用ルール
 
### INDEX.md ファイル
  * ルート、docs/、および docs/直下のフォルダには必ず INDEX.md ファイルを置く
  * INDEX.md ファイルには同階層および第１階層までの*.mdファイルへのリンクと簡単な説明を記述する
  * INDEX.md ファイルには文書の最初と最後に上位階層にある INDEX.md へのリンクをつける
    * 末尾の mdファイルの所在キャプションの２つ上の行には `**━━━━━━━━━━━━━━━━━━━━━━━━**
` を入れる
    * 冒頭の mdファイルの所在キャプションの２つ下の行には `**━━━━━━━━━━━━━━━━━━━━━━━━**
` を入れる
  * リンク間違いやリンク切れを見つけたときは適宜修正すること
 
 
### INDEX.md ファイル以外の mdファイル
  * 文書の第最初と最後に同階層またはルート階層にある INDEX.md へのリンクをつける
  * プロジェクト内のmdファイル中に、一行目や最終行にINDEX.md へのリンクのないファイルを見つけた場合は、INDEX.mdファイルへのリンクを貼る 
  * プロジェクト内のmdファイル中に、一行目や最終行のINDEX.md へのリンクが間違っているファイルを見つけた場合は、INDEX.mdファイルへのリンクを修正する 


### mdファイルの所在キャプション
  * すべての md ファイル（INDEX.md を含む）の冒頭と末尾に、プロジェクトルートからの相対パスをイタリックで記述する
  * 冒頭: INDEX リンクの次の行（空行を挟む）にパスを置き、その次の行（空行を挟む）に `**━━━━━━━━━━━━━━━━━━━━━━━━**` を置く。
  * 末尾: `**━━━━━━━━━━━━━━━━━━━━━━━━**` の次の行（空行を挟む）にパスを置き、その次の行（空行を挟む）に INDEX リンクを置く
  * 例（`20260303_update-of-the-mechanism-a/plans.md` の場合）:
    ```
    [INDEX](./INDEX.md)
 
    *20260303_update-of-the-mechanism-a/plans.md*
 
    **━━━━━━━━━━━━━━━━━━━━━━━━**
 
    # タイトル
 
    本文
 
    **━━━━━━━━━━━━━━━━━━━━━━━━**
 
    *20260303_update-of-the-mechanism-a/plans.md*
 
    [INDEX](./INDEX.md)
    ```
 
### docs/ フォルダの命名規則
 
#### フォルダ名
  * 形式は `YYYYMMDD_<slugged-description>` とする
  * `YYYYMMDD` は、そのフォルダに含まれる最古ファイルの日付とする
  * `<slugged-description>` は英小文字の kebab-case（3-8語推奨）とする
  * 禁止事項:
    * 末尾アンダースコア（例: `research_`）
    * 連番の埋め込み（例: `_01_`, `_02_`）
    * 大文字混在（`DokuWiki` などは `dokuwiki` に正規化）
    * 意味が広すぎる説明（`misc` の多用）
 
#### docs/ 配下の Markdown ファイル名
  * 形式は `<kind>_<slugged-description>.md` または `<kind>.md`とする
  * `<kind>` は以下の統合語彙から選ぶ:
    * `research` - 技術調査、文献調査、比較検討
    * `plan` - 計画、作業分解、ToDo
    * `note` - 断片メモ、気づき、短文記録
    * `analysis` - 解析ノート、解析結果の考察
    * `log` - 作業ログ
    * `dataset` - データ定義、前処理条件、変数情報
    * `spec` - 仕様
    * `decision` - 意思決定記録
    * `protocol` - 実験/解析の手順、実施条件
    * `report` - 実施結果の報告
  * 旧 `tweet` は `note` に統合する
  * 迷う場合は `note` を優先し、細分化しすぎない
 
#### docs/ 各フォルダの推奨構成
  * `INDEX.md`（必須）
  * `files/`（画像・PDF・添付資料。任意）
 
----
 
## 「注釈に対応して」と依頼された場合
 
  * 注釈は主にユーザーがAIに文書に対して追記や修正を依頼するときに用いることとする
  * 「注釈に対応して」と依頼された場合は、その回答は注釈を使わずに本文の中に組み込んだ形で行うこと
